<section id="work" class="work">
    <div class="grid">
        <div class="grid-list">
            {% set projects = work.projects %}
            {% for project in projects %}
                <div class="grid-item">
                    {% if project.image %}
                        <img src="{{ project.image }}" alt="{{ project.alt }}"/>
                    {% endif %}
                    {% if project.title %}
                        {% if project.pdf %}
                            <a href="{{ project.pdf }}" target="_blank">{% markdown %}{{ project.title }}{% endmarkdown %}</a>
                        {% else %}
                            {% markdown %}{{ project.title }}{% endmarkdown %}
                        {% endif %}
                    {% endif %}
                    {% markdown %}
                    {{ project.body }}
                    {% endmarkdown %}
                </div>
            {% endfor %}
        </div>
    </div>
</section>